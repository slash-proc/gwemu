/GNUmakefile
/build/
/rm0455.pdf
/STM32H7B0.svd
/sdk/
/.cache/
/.vscode/
*.pyc
.sdk
.stgit-*
.git-submodule-status
.clang-format
.gdb_history
cscope.*
tags
TAGS
GPATH
GRTAGS
GTAGS
*~
*.ast_raw
*.depend_raw
*.swp
*.patch
*.gcov

# gnw: Claude Code's own session state (worktrees, mailbox, scheduled
# tasks, etc.) -- never part of the repo, but only excluded on this
# machine via .git/info/exclude until now; make it travel with the repo.
/.claude/

# gnw: scratch firmware images for boot testing, not part of the repo
/retro-go-temp/
/gnw_chainloader.bin
/example-extflash-backup.bin
/build-release/
/sdcard.img
/*.bin
/perf.data
/backup/
/GEMINI.md
*.mp3
*.qcow2
*.7z
